## Backend environment variables for local development
# Replace the placeholder values with your real secrets before running the server.

# MongoDB connection string (required). Example for local MongoDB:
# MONGO_URI=mongodb://localhost:27017/group6db
# Example for MongoDB Atlas (replace <user>, <pass>, <cluster>, <dbname>):
# MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://nhom_6:0123456789@cluster0.hnybrg4.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Server port (optional)
PORT=8080

# App environment
NODE_ENV=development

# JWT secrets (replace with strong random strings)
JWT_SECRET=change_this_jwt_secret
REFRESH_TOKEN_SECRET=change_refresh_secret

# Email (used for sending confirmation/reset emails)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your@email.com
EMAIL_PASS=your-email-password
EMAIL_FROM=your@email.com

# Cloudinary (if you use avatar uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Client URL used when building links in emails
CLIENT_URL=http://localhost:3000
